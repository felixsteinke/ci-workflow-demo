name: CI Pipeline 1

on:
  # manual trigger
  workflow_dispatch:
  # trigger on every push
  push:

jobs:
  placeholder-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
